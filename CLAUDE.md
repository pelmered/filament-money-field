# CLAUDE.md

@AGENTS.md

The project guidance lives in `AGENTS.md`, which the line above imports. Keeping
one file means Claude Code, Codex and every other agent read the same thing —
these two files had already drifted apart on which Filament versions are
supported. Make changes there, not here.
